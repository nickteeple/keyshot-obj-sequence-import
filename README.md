This script imports a sequence of OBJ files into Keyshot and renders them using the current camera and lighting settings, applies a specified material template, and allows for various other options. 
